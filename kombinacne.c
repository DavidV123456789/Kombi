#include "kombinacne.h"

int getKombinacne (int n, int k){
	
	int kombinacne;
	int r
	
	r = K.n - K.k;
	
	kombinacne = getFaktorial (K.n) / ( getFaktorial (r) * getFaktorial (K.k) );
	
	return kombinacne;
	
	}
